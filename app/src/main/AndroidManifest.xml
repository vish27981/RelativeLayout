<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/evil"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/evil"
        android:supportsRtl="true"
        android:theme="@style/Theme.RelativeLayout"
        tools:targetApi="31">
        <activity
            android:name=".customAlert"
            android:exported="true" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity
            android:name=".MenuActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".ToastActivity"
            android:exported="true"></activity>

        <activity
            android:name=".ConstraintLayoutActivity"
            android:exported="true" />

        <activity
            android:name=".LinearLayoutVertical"
            android:exported="true" />

        <activity
            android:name=".LinearLayout"
            android:exported="true">

        </activity>

        <activity
            android:name=".RelativeUI"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>